Read `UiAutomationGRPC.AI/Skill/UiAutomationSkill/SKILL.md` and follow its instructions to automate the requested Windows application.

The `apps/` subdirectory (`UiAutomationGRPC.AI/Skill/UiAutomationSkill/apps/`) contains per-app mental maps — load the relevant one before navigating.
